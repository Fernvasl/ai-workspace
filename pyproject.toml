[project]
name = "ai-workspace"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Tu Nombre", email = "fernandovasquez763@gmail.com" }
]
requires-python = ">=3.13"
dependencies = []

[project.scripts]
ai-workspace = "ai_workspace:main"

[build-system]
requires = ["uv_build>=0.12.3,<0.13.0"]
build-backend = "uv_build"
